# Unit Test Templates

โฟลเดอร์นี้เก็บ Template ของ Unit Test สำหรับแต่ละโปรเจ็ค

## โครงสร้างไฟล์

วางไฟล์ Template ตามชื่อโปรเจ็ค:

```
template-unittest/
├── AdaPos5.0-Retail.xlsx          # Template สำหรับ AdaPos5.0-Retail
├── AdaPos5.0-Retail-Agency.xlsx   # Template สำหรับ AdaPos5.0-Retail-Agency
├── AdaScriptCenter.xlsx           # Template สำหรับ AdaScriptCenter
└── default.xlsx                   # Template เริ่มต้น (ถ้าไม่พบไฟล์ที่ตรงกับโปรเจ็ค)
```

## รูปแบบไฟล์ที่รองรับ

- `.xlsx` - Excel (แนะนำ)
- `.xls` - Excel รุ่นเก่า
- `.docx` - Word
- `.doc` - Word รุ่นเก่า
- `.pdf` - PDF

## วิธีใช้งาน

1. **สร้างไฟล์ Template** ตามชื่อโปรเจ็ค (ต้องตรงกับชื่อใน Configuration)
2. **วางไฟล์ในโฟลเดอร์นี้**
3. **เลือกโปรเจ็ค** ในหน้า Deploy Tool
4. **คลิกปุ่ม "📥 Unit Test"** ที่ header ของ Google Drive Links
5. **ไฟล์จะดาวน์โหลดอัตโนมัติ**

## ตัวอย่างการตั้งชื่อไฟล์

```
AdaPos5.0-Retail.xlsx              ✅ ถูกต้อง
AdaPos5.0-Retail-Agency.xlsx       ✅ ถูกต้อง
AdaScriptCenter.xlsx               ✅ ถูกต้อง
default.xlsx                       ✅ ถูกต้อง (Fallback)

adapos5.0-retail.xlsx              ❌ ผิด (ตัวพิมพ์ไม่ตรง)
AdaPos 5.0 Retail.xlsx             ❌ ผิด (มีช่องว่าง)
```

## หมายเหตุ

- ชื่อไฟล์ต้อง**ตรงกับชื่อโปรเจ็ค**ทุกตัวอักษร (Case-sensitive)
- ถ้าไม่พบไฟล์ที่ตรงกับโปรเจ็ค ระบบจะใช้ `default.xlsx`
- ถ้าไม่มีไฟล์เลย ระบบจะแจ้งเตือน "Template not found"
